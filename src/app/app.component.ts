import { Component } from '@angular/core';

@Component({
  moduleId: 'module.id',
  selector: 'app-root',
  template: `
  {{title}}
  `,
  styles: [`

  `]
})
export class AppComponent {
  title = 'Running...';
}
